services:
  - type: web
    name: structural-monitoring-app
    envVars:
      - key: NODE_ENV
        value: production
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html